name: Access Environment-Specific Secret

on:
  push:
    branches:
      - main

jobs:
  use-env-secret:
    runs-on: ubuntu-latest

    steps:
      - name: Try new script to start android emulator
        uses: ThangNguyen0495/Customs@e1.1.0
        with:
          api-level: '35'
