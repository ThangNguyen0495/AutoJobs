name: Access Environment-Specific Secret

on:
  push:
    branches:
      - main

jobs:
  use-env-secret:
    runs-on: ubuntu-latest

    steps:
      - name: Try new script to start android emulator
        uses: ThangNguyen0495/Customs@e.1.1.1
        with:
          api-level: '35'
